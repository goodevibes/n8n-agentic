# MCP Server Configuration
# The MCP server command and arguments to run (e.g., npx n8n-mcp)
MCP_SERVER_COMMAND=npx
MCP_SERVER_ARGS=n8n-mcp

# n8n Instance Configuration
# URL of your n8n instance that the agent will connect to
MCP_SERVER_ENV_N8N_API_URL=http://localhost:5678

# n8n API Key
# Generate this from your n8n Settings > API
MCP_SERVER_ENV_N8N_API_KEY=your-n8n-api-key-here

# AI Provider (choose one)
# Anthropic API Key (for Claude models)
ANTHROPIC_API_KEY=your-anthropic-key-here

# OR OpenAI API Key (for GPT models)
# OPENAI_API_KEY=your-openai-key-here

# Optional: Server Configuration
# PORT=8000
# HOST=0.0.0.0
